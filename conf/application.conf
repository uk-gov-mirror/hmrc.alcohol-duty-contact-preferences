# Copyright 2026 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

include "backend.conf"

# This is the main configuration file for the application.
# ~~~~~

appName = alcohol-duty-contact-preferences

# Default http client
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.HttpClientV2Module"

# Play Modules
play.modules.enabled += "uk.gov.hmrc.alcoholdutycontactpreferences.config.Module"

play.modules.enabled += "uk.gov.hmrc.mongo.play.PlayMongoModule"

play.modules.enabled += "uk.gov.hmrc.play.bootstrap.AuthModule"

# The application languages
# ~~~~~
play.i18n.langs = ["en"]

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# !!!WARNING!!! DO NOT CHANGE THIS ROUTER
play.http.router = prod.Routes

# Controller
# ~~~~~
# By default all controllers will have authorisation, logging and
# auditing (transaction monitoring) enabled.
# The below controllers are the default exceptions to this rule.

# Microservice specific config

mongodb {
  uri = "mongodb://localhost:27017/alcohol-duty-contact-preferences"
}

microservice {
  services {
    auth {
      host = localhost
      port = 8500
    }

    subscription {
      protocol = http
      host = localhost
      port = 16004
      clientId = "subscription stub client id"
      secret   = "override-me!"
      url {
        subscriptionSummary = "/etmp/RESTAdapter/excise/subscriptionsummary"
      }
    }

    email-verification {
      protocol = http
      host = localhost
      port = 9891
      url {
        getVerifiedEmails = "/email-verification/verification-status"
      }
    }

    alcohol-duty-stubs {
      protocol = http
      host = localhost
      port = 16004
    }

    submit-preferences {
      protocol = http
      host     = localhost
      port     = 16004
      clientId = "submission stub client id"
      secret   = "override-me!"
      url {
        submitPreferences = "/etmp/RESTAdapter/email-contact-preference"
      }
    }

    circuit-breaker {
      max-failures  = 5
      call-timeout  = 10 seconds
      reset-timeout = 1 minute
    }

    retry {
      retry-attempts = 2
      retry-attempts-post = 1
      retry-attempts-delay = 500 milliseconds
    }
  }
}

downstream-apis {
  idType = "ZAD"
  regime = "AD"
}

enrolment {
  serviceName = "HMRC-AD-ORG"
  identifierKey = "APPAID"
}

crypto {
  key = "wuvdSGFABMr6sTfG0GRUpyqH3nzZM0ORjNviFgWof9M="
  isEnabled = true
}

features {
  email-verification-stub: true
}
